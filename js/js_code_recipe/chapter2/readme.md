# 20. 진위 여부 판별하기
- 브라우저 버전에 따라 알림창을 띄우고 싶을 때
- 입력 항목에 따라 확인 버튼을 무효화하고 싶을 때
- 진릿값에 '!'를 붙이면 부정 연산
- '!!' boolean 으로 변환

# 21. 숫자 다루기
- 자바스크립트에서 수를 계산하고 싶을 때
    - 수학, 어림, 삼각함수, 임의의 수, 진수 등등
    - 'Nan', 'infinity' : 수의 계산이 부정확한 계산 결과에 따라 반환되는 특별한 값

# 22. 어림 계산하기
- 소수점을 버리는 계산을 할 때
- 화면 크기를 정수로 조절할 때
    - Math.round() : 반올림(0.5 이상)
    - Math.floor() : 버림
    - Math.ceil() : 올림
    - Math.trunc() : 정수 부분만 반환

# 23 임의의 수 다루기
- 임의의 확률을 사용해 작업을 처리하고 싶을 때
- 애니메이션에 임의의 값을 부여하고 싶을 때
    - Math.random() : 0 이상 1 미만의 난수
    - '``' 변수를 안에 넣을 때 사용
    - 비밀번호 등 안전한 난수 배열 생성은
        - crypto.getRandomValues(타입지정배열) : 난수의 배열을 반환

# 24. 수학 계산 함수 사용하기
- 절댓값을 구하고 싶을 때
- 제곱을 구하고 싶을 때
- 대수를 구하고 싶을 때
    - Math.abs(값) : 절댓값을 계싼
    - Math.pow(값1, 값2) : 값1의 값2승을 계산
    - Math.sign(값) : 값의 부호 계산(-10은 -1, 21은 1, 0은 0)
    - Math.sqrt(값) : 제곱근을 계산
    - Math.log(값) : 자연 로그를 계산
    - Math.exp(값) : 지수 함수를 계산
    - Math.E : 자연로그 e를 반환

# 25. 삼각 함수 사용하기
    - 삼각 함수 계산을 사용하고 싶을 때
    - 좌표에서 각도를 구하고 싶을 때
    - 호를 그리는 애니메이션 효과를 주고 싶을 때
        - Math.PL : 원주율
        - Math.cos(값) : 코사인(cos)
        - Math.sin(값) : 사인(sin)
        - Math.tan(값) : 탄젠트(tan)
        - Math.acos(값) : 코사인 역함수(acos)
        - Matg.asin(값) : 사인 역함수(asin)
        - Math.atan(값) : 탄젠트 역함수(atan)
        - Math.atan2(y좌표, x좌표) : (x,y) 좌표가 이루는 각도
        - 라디안 = (도수 * Math.PI) / 180;
            - 라디안은 각의 크기를 재는 SI 단위이다. 호도라고도 하며, 기호는 rad이다. 단위원의 중심각의 라디안 값은 그 각이 대하는 호의 길이와 같다. 1라디안은 약 57.3도이다.

# 26. 문자열 다루기
    - 문자열 길이를 확인하고 싶을 때
    - 문자열을 검색, 추출, 변환하고 싶을 때
    - 문자열을 나누고, 결합하고 싶을 때
    - 특수 문자 사용시
        - \' : 작은따옴표
        - \" : 큰따옴표
        - \` : 백틱
        - \\ : 역슬래쉬
        - \n : 줄바꿈
        - \r : 되돌리기

# 27. 문자열의 길이 확인하기
    - 입력폼에서 문자열의 길이를 확인하고 싶을 때
    - 구문
        - 문자열.length : 문자열의 길이
        - Array.from(문자열).length : 문자열의 길이

# 28. 문자열의 공백 처리하기
    - 문자열 양단의 공백을 제거하고 싶을 때
    - 구문
        - 문자열.trim() : 문자열 양 끝의 공백을 제거
        - \n도 제거함

# 29. 문자열 검색하기(인덱스 검색)
    - 원하는 문자열의 위치를 확인하고 싶을 때
    - 구문
        - 대상문자열.indexOf(검색대상문자열, [검색시작인덱스])
        - 대상문자열.lastIndexOf(검색대상문자열, [검색시작인덱스])
        - 대상문자열.search(정규표현)
        - 대소문자 구별
        - 나중에 정규표현식 확인할 것

# 30. 문자열 검색하기(부분 검색)
    - 지정 문자열의 포함 여부를 확인하고 싶을 때
    - 입력폼의 부적절한 문자를 체크하고 싶을 때
        - 구문
            - 대상문자열.includes(검색대상문자열, [검색시작인덱스])
            - 대상문자열.startsWith(검색대상문자열, [검색시작인덱스])
            - 대상문자열.endsWith(검색대상문자열, [검색시작인덱스])
            - 불리안으로 결과값이 나옴

# 31. 문자열에서 문자 다루기(문자열에서 문자 추출하기)
    - 지정한 위치의 문자를 추출하고 싶을 때
    - 구문
        - 문자열.charAt([인덱스]) : 지정한 인덱스의 문자를 추출
        - * html, css, js 꼭 복습하기 *

# 32. 문자열 다루기(위치 지정하여 선택하기)
    - 지정 범위 내의 문자열을 추출하여 사용하고 싶을 때
    - 지정 위치 이후의 문자열을 추출하여 사용하고 싶을 때
    - 구문
        - 문자열.slice(시작인덱스, 종료인덱스)
        - 문자열.substring(시작인덱스, 종료인덱스)
        - slice는 음의 정수 사용 가능
        - 두가지 차이점은 substring 은 시작 값이 종료 값보다 클 경우 두 인덱스를 바꿔 작업을 처리한다.

# 33. 문자열 다루기(글자 수 지정하여 선택하기)
    - 위치와 글자 수를 지정한 문자열을 추출하고 싶을 때
    - 문자열.substr(시작인덱스, 글자수);

# 34. 지정 문자열 변환하기
    - 문자열을 다른 문자열로 바꾸고 싶을 때
    - 문자열 내 불필요한 줄바꿈 코드를 <br>로 바꾸고 싶을 때
    - 빈칸을 제거하고 싶을 때
    - 구문
        - 대상문자열.replace(문자열1, 문자열2)
        - 대상문자열.replace(정규표현, 문자열)

# 35. 문자열 나누기
    - URL의 해시 데이터를 가져올 때
    - 공백을 기준으로 문자열을 나누고 싶을 때
    - 구문
        - 문자열.split(기준문자, 최대분할수)
        - 문자열.split(정규표현, 최대분할수)
        - 연습문제 다시볼 것

# 36. 문자열 합치기
    - 따로 입력받은 성과 이름을 결합하여 문자열을 만들고 싶을 때
    - 폴더명과 파일명을 결합하여 파일 주소를 문자열로 만들고 싶을 때
    - 구문
        - 문자열1 + 문자열2 + 문자열3 ... : 결합
        - `${문자열1}${문자열2}${문자열3}` : 결합

# 37. 문자열의 대소문자 변환하기
    - 소문자를 대문자로 변환하고 싶을 때
    - 서버에서 반환된 대문자 데이터를 소문자로 변화하고 싶을 때
    - 구문
        - 문자열.toLowerCase()
        - 문자열.toUpperCase()

# 38. 문자열과 식 함께 다루기
    - 여러 문자열을 다루고 싶을 때
    - 문자열 내 변수를 다루고 싶을 때

# 39. 정규 표현식 사용하기
    - 조건과 일치하는 문자열을 검색하고 싶을 때
    - 많은 문자열을 패턴에 따라 일괄적으로 변환하고 싶을 때

# 40. 정규 표현식으로 특정 문자 검색하기
    - 조건과 일치하는 문자열을 검색하고 싶을 때
    - 구문
        - /패턴/.test(문자열)

# 41. 소수점 자릿수 지정하기
    - 숫자 3.14...를 문자열 '3.14'로 변환하고 싶을 때
    - 숫자 10을 문자열 '10.00'로 변환하고 싶을 때
    - 구문
        - 숫자값.toFixed(자릿수)
        - 숫자값.toPrecision(자릿수)

# 42. 문자열 길이 맞추기
    - 10미만의 숫자 앞에 0을 붙여 두자리 형식을 만들고 싶을 때
    - 구문
        - 문자열.padStart(전체길이, 추가문자열)
        - 문자열.padEnd(전체길이, 추가문자열)

# 43. 문자열 URL 이스케이프 처리하기
    - url의 한글을 인코딩할 때
    - SNS의 한글을 인코딩 후 URL로 변환하고 싶을 때
    - 구문
        - encodeURI(문자열)
        - encodeURIComponent(문자열)

# 44. 문자열 URI 디코드 하기
    - 인코딩된 URI 문자열을 디코딩하고 싶을 때
    - 구문
        - decodeURI(문자열)
        - decodeURIComponent(문자열)