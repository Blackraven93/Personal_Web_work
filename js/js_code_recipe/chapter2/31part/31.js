/* let searchValue = document.querySelector('.search');

const allTd = document.querySelectorAll('td');

const tdNumber = document.getElementsByTagName('td');




function collectF() {

    for (let index = 0; index < tdNumber.length; index++) {
        let element = tdNumber[index].innerText;
        if (element.charAt() != searchValue.value) {
            tdNumber[index].style.display = 'none';
        } else if (searchValue.value == '') {
            allTd.style.display = "block";
        } else {
            tdNumber[index].style.display = 'block';
        }

    }

}



const onKeyUp = () => {
    collectF()
}

addEventListener('keyup', onKeyUp); */

// 내가한거랑 비교해보기


